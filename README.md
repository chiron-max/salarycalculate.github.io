你是一名资深前端工程师和交互设计师。请为我设计并实现一个“月度工作时长统计网站”，要求使用 HTML、CSS、JavaScript（可选使用少量现代前端语法，但不要依赖复杂后端）。请直接输出完整可运行代码，并尽量做成单页应用，优先考虑可维护性、可扩展性和交互体验。

项目目标：
1. 解决“每个工种工作方式不同”的问题，支持灵活记录工作时间。
2. 允许夜班、跨天班次、临时加班、补班、半天班、碎片化工时等复杂情况。
3. 不要求用户每天手动输入上班/下班时间，而是使用“可视化时间轴上的工作时长条段”来编辑和记录。
4. 自动统计每月总工时、每日工时、加班时长、夜班时长，并支持按月份切换查看。

核心交互设计：
1. 将“白班”即“早9晚5”、夜班即“晚8到早8”等不同时间段的工作，理解成一个“时间package”的概念，用户可以自由地给每一天添加或删除“时间package”，或者快捷地多选日期以添加“时间package”

统计功能：
1. 自动计算每月总工时。
2. 显示月度汇总卡片：
   - 总工时
   - 正常工时
   - 加班工时
   - 夜班工时
   - 工作天数
3. 提供可导出数据为 JSON / CSV 的功能。


数据设计：
1. 请设计清晰的数据结构，例如：
   - month
   - day
   - segments
   - segmentType
   - startMinute
   - endMinute
   - note
   - profileId
2. 数据模型要便于后续扩展到后端数据库。

UI/UX 要求：
1. 风格简洁、专业、现代，偏 Apple 风格。
2. 移动端和桌面端都要可用，尽量响应式。
3. 对空状态、无数据状态、错误输入要有良好处理。

技术要求：
1. 尽量用原生 HTML/CSS/JS 实现，不依赖重型框架。
2. 代码结构要清晰，建议拆分为：
   - index.html
   - styles.css
   - app.js
3. 如果为了演示方便，也可以输出一个单文件版本，但代码必须整洁。
4. 需要包含必要注释，便于我后续二次开发。
5. 确保核心逻辑完整，不要只做静态界面。


请直接开始输出完整代码，并在代码后简要说明：
1. 核心数据结构
2. 主要交互逻辑
3. 后续可以扩展的方向

额外要求：
- 优先考虑真实可用性，不要只输出概念性代码。
- 尽量减少样板代码，集中体现核心实现。
- 如果实现复杂交互有困难，请优先完成“时间轴条段 + 统计 + 本地存储 + 配置切换”这四个核心部分。
